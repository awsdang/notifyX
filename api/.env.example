# ===========================================
# NotifyX API Configuration
# ===========================================

# Server
PORT=3000
NODE_ENV=production
TRUST_PROXY=true

# CORS (comma-separated origins, or * for all)
CORS_ORIGIN=https://notification.sadeek.app
# Optional (unsafe): disable CORS checks entirely. Keep false in production.
# CORS_DISABLE=false

# ===========================================
# Database (Required)
# ===========================================
DATABASE_URL="postgresql://notifyxu:YOUR_POSTGRES_PASSWORD@notifyx-notifyx-4vxsxn:5432/notifyx?schema=public"

# Connection Pooling
DATABASE_POOL_SIZE=10
DATABASE_CONNECTION_TIMEOUT=10000

# ===========================================
# Redis (Required)
# ===========================================
REDIS_URL="redis://default:YOUR_REDIS_PASSWORD@notifyx-redis-abvapv:6379"
# Set to true for local API-only development without Redis/BullMQ.
# Queue-backed delivery + scheduler fan-out will be disabled.
# REDIS_DISABLED="true"

# ===========================================
# Security (Required for Production)
# ===========================================

# API Key Authentication (comma-separated for multiple keys)
API_KEYS="your-api-key-here,another-api-key"

# Credential Encryption Key (REQUIRED — at least 32 characters)
# Generate with: openssl rand -base64 48
CREDENTIAL_ENCRYPTION_KEY="CHANGE-ME-generate-with-openssl-rand-base64-48"

# Admin Bootstrap Token (recommended for production)
# If set, the initial POST /admin/setup requires header: X-Setup-Token: <value>
# Generate with: openssl rand -hex 32
# ADMIN_SETUP_TOKEN=""

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# Worker Configuration
# ===========================================
NORMAL_WORKER_CONCURRENCY=5
NORMAL_WORKER_RATE_LIMIT=50
HIGH_WORKER_CONCURRENCY=20
HIGH_WORKER_RATE_LIMIT=500

# Scheduler poll interval (ms) — applies when running multiple API instances
SCHEDULER_POLL_INTERVAL=10000
SCHEDULER_BATCH_SIZE=100
# Distributed lock TTL (ms) — should exceed max tick duration
SCHEDULER_LOCK_TTL_MS=30000

# ===========================================
# Firebase Cloud Messaging (FCM)
# OPTIONAL: Global fallback when app has no credentials
# For production, configure per-app via API
# ===========================================
# FCM_PROJECT_ID="your-firebase-project-id"
# FCM_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com"
# FCM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ===========================================
# Huawei Mobile Services (HMS)
# OPTIONAL: Global fallback when app has no credentials
# ===========================================
# HMS_APP_ID="your-huawei-app-id"
# HMS_APP_SECRET="your-huawei-app-secret"

# ===========================================
# Apple Push Notification Service (APNS)
# OPTIONAL: Global fallback when app has no credentials
# ===========================================
# APNS_KEY_ID="YOUR_KEY_ID"
# APNS_TEAM_ID="YOUR_TEAM_ID"
# APNS_BUNDLE_ID="com.yourapp.bundle"
# APNS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_P8_KEY_HERE\n-----END PRIVATE KEY-----"
# APNS_PRODUCTION="false"

# ===========================================
# Web Push (VAPID)
# OPTIONAL: Global fallback for browser notifications
# Generate keys: npx web-push generate-vapid-keys
# ===========================================
# VAPID_PUBLIC_KEY="your-vapid-public-key"
# VAPID_PRIVATE_KEY="your-vapid-private-key"
# VAPID_SUBJECT="mailto:admin@yourdomain.com"

# ===========================================
# Object Storage (MinIO/S3)
# ===========================================
MINIO_ENDPOINT="storage.sadeek.app"
MINIO_PORT=443
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="YOUR_MINIO_PASSWORD"
MINIO_USE_SSL=true
MINIO_BUCKET="notifyx-assets"
# IMPORTANT:
# MINIO_ENDPOINT is the external host fallback (and can be used by host-run API).
# Internal writes should go through Dokploy service DNS below.
MINIO_INTERNAL_ENDPOINT="notifyx-minio-pukvix"
MINIO_INTERNAL_PORT=9000
MINIO_INTERNAL_USE_SSL=false
# Set true to disable endpoint fallback and only use the primary endpoint.
MINIO_DISABLE_INTERNAL_FALLBACK=false
# Optional public base URL for uploaded assets (required for real devices)
# Example: ASSET_PUBLIC_BASE_URL="https://assets.yourdomain.com"
# If the portal and API live on notification.sadeek.app, keep this as a
# real public asset host and not the internal MinIO container name.
ASSET_PUBLIC_BASE_URL="https://storage.sadeek.app/api/v1/"

# ===========================================
# Logging
# ===========================================
LOG_LEVEL="info"
LOG_FORMAT="json"

# ===========================================
# Dead Letter Queue
# ===========================================
DLQ_MAX_RETRIES=5
DLQ_ALERT_THRESHOLD=100
