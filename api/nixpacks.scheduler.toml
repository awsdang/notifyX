[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22", "bun", "openssl"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run db:generate", "bun run build"]

[start]
cmd = "bun run start:scheduler"
