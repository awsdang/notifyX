[variables]
NODE_ENV = "production"
NIXPACKS_SPA_CADDY = "true"
NIXPACKS_SPA_OUT_DIR = "dist"

[phases.setup]
nixPkgs = ["nodejs_latest"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]
